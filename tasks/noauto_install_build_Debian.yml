---
- name: Ensure we are using apt
  ansible.builtin.fail:
    msg: "This role only supports {{ ansible_os_family }} systems that use apt as a package manager."
  when: ansible_pkg_mgr != "apt"

- name: Install wireshark dependencies via apt
  block:
    - name: Update apt cache
      ansible.builtin.apt:
        update_cache: true
        cache_valid_time: 3600
      become: true

    - name: Ensure wireshark build pkgs are installed
      ansible.builtin.apt:
        name: "{{ package }}"
        state: present
      loop: "{{ wireshark_build_pkgs }}"
      loop_control:
        loop_var: package
      become: true
